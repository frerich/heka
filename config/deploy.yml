service: heka

image: frerich/heka

servers:
  web:
    - 178.105.198.6

proxy:
  ssl: true
  host: heka.idkfa.site

registry:
  server: localhost:5555

builder:
  arch: amd64

env:
  secret:
    - SECRET_KEY_BASE
